"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.lastQueryResultSemigroup = exports.keepQueryResultSemigroup = void 0;
var QR = require("../QueryResult");
function keepQueryResultSemigroup() {
    return {
        concat: function (a, b) {
            return a._tag === 'Success' && b._tag === 'Loading'
                ? QR.queryResultSuccess(a.success, true)
                : a._tag === 'Failure' && b._tag === 'Loading'
                    ? QR.queryResultFailure(a.failure, true)
                    : b;
        }
    };
}
exports.keepQueryResultSemigroup = keepQueryResultSemigroup;
function lastQueryResultSemigroup() {
    return {
        concat: function (_, b) { return b; }
    };
}
exports.lastQueryResultSemigroup = lastQueryResultSemigroup;
